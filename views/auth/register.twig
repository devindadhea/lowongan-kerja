<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Daftar Akun</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #F1F1F1;
            font-family: 'Segoe UI', sans-serif;
        }
        .bg-primary-custom {
            background-color: #1D3557;
        }
        .text-primary-custom {
            color: #1D3557;
        }
        .text-muted-custom {
            color: #2B2D42;
        }
        .btn-primary-custom {
            background-color: #1D3557;
            color: #fff;
        }
        .btn-primary-custom:hover {
            background-color: #457B9D;
        }
        .link-custom {
            color: #457B9D;
        }
    </style>
</head>
<body>
<div class="row g-0" style="min-height: 100vh;">
    <!-- Kolom Kiri -->
    <div class="col-md-6 d-flex flex-column justify-content-center align-items-center text-white px-4 bg-primary-custom">
        <h3 class="text-center fw-bold mb-2">Sistem Informasi Lowongan Kerja</h3>
        <p class="text-center">Aplikasi Slim Framework + Medoo + Twig</p>
    </div>

    <!-- Kolom Kanan -->
    <div class="col-md-6 d-flex align-items-center justify-content-center bg-white">
        <div style="width: 100%; max-width: 400px;">
            <h3 class="text-center mb-2 fw-bold text-primary-custom">Daftar Akun</h3>
            <p class="text-center mb-4 text-muted-custom">Buat akun untuk mencari lowongan kerja</p>

            {% if error %}
                <div class="alert alert-danger text-center rounded-pill py-2">
                    {{ error }}
                </div>
            {% endif %}

            <form method="POST" action="{{ base_path() }}/register">
                <div class="mb-3">
                    <label class="form-label text-muted-custom">Nama</label>
                    <input type="text" name="nama" class="form-control rounded-pill border" placeholder="Masukkan nama lengkap" required style="border-color: #E0E0E0;">
                </div>
                <div class="mb-3">
                    <label class="form-label text-muted-custom">Email</label>
                    <input type="email" name="email" class="form-control rounded-pill border" placeholder="Masukkan email" required style="border-color: #E0E0E0;">
                </div>
                <div class="mb-3">
                    <label class="form-label text-muted-custom">Password</label>
                    <input type="password" name="password" class="form-control rounded-pill border" placeholder="Masukkan password" required style="border-color: #E0E0E0;">
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-lg rounded-pill fw-semibold btn-primary-custom">
                        DAFTAR
                    </button>
                </div>
            </form>

            <p class="mt-4 text-center text-muted-custom">Sudah punya akun?
                <a href="{{ base_path() }}/login" class="link-custom">Login di sini</a>
            </p>
        </div>
    </div>
</div>
</body>
</html>
