{% extends "master.twig" %}

{% block content %}
<div class="container mt-5 pl-20">
    <h2 class="mb-4 text-primary">👋 Selamat datang, {{ jobseeker.nama|default('Pengguna') }}</h2>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-white p-4 rounded shadow text-center">
            <h5 class="text-lg font-bold text-blue-600">Total Lamaran</h5>
            <p class="text-3xl">{{ totalLamaran }}</p>
        </div>

        <div class="bg-white p-4 rounded shadow text-center">
            <h5 class="text-lg font-bold text-blue-600">Status CV</h5>
            {% if jobseeker.cv %}
                <p class="text-green-600">✅ Ter-upload</p>
                <a href="{{ base_path() }}/uploads/cv/{{ jobseeker.cv }}" target="_blank" class="text-blue-600 text-sm">Lihat CV</a>
            {% else %}
                <p class="text-red-600">Belum upload</p>
            {% endif %}
        </div>

        <div class="bg-white p-4 rounded shadow text-center">
            <h5 class="text-lg font-bold text-blue-600">Aksi Cepat</h5>
            <div class="flex flex-col gap-2 mt-2">
                <a href="{{ base_path() }}/user/applications" class="text-sm text-primary hover:underline">📄 Riwayat Lamaran</a>
                <a href="{{ base_path() }}/user/profile" class="text-sm text-primary hover:underline">👤 Edit Profil</a>
                <a href="{{ base_path() }}/user/password" class="text-sm text-primary hover:underline">🔒 Ganti Password</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
